import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // TODO(client): Add the image hostnames used by this project.
    // Example: { protocol: "https", hostname: "res.cloudinary.com" }
    remotePatterns: [],
  },
};

export default nextConfig;
